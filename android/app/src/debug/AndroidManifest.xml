<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.untitled1">

    <!-- Any specific overrides for debug builds can go here. No need to define general attributes again -->

    <!-- Optional: Include any debug-specific configurations, like different API keys, etc. -->
    <application>
        <!-- Debug-specific meta-data or settings can go here, such as enabling debug features -->
        <!-- Example: Enabling debug logging, different API keys, etc. -->
    </application>

</manifest>
